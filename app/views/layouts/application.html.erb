<!DOCTYPE html>
<html>
  <head>
    <title>MaiKusuri</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body data-controller="toggle">
    <%= render "shared/navbar" %>
    <div class="container-fluid">
      <div class="row">
        <%# Replaced 'd-none' with 'collapsed' for the transition %>
        <nav class="col-md-2 sidebar pt-3 border-end collapsed" data-toggle-target="hideable">
          <%= render "shared/sidenav", challenges: @chats %>
        </nav>
        <main class="col-md-12 my-4 mb-0 px-0" id="main-content" data-toggle-target="content">
          <%= yield %>
        </main>
      </div>
    </div>
    <div class="fixed-bottom p-3" style="pointer-events: none;">
      <% if notice %>
        <p class="alert alert-info" style="pointer-events: auto;"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-warning" style="pointer-events: auto;"><%= alert %></p>
      <% end %>
    </div>
  </body>
</html>
